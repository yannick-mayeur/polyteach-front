Toutes les commandes que j'ai du faire avant de pouvoir run:

```
npm install webpack-dev-server -g
npm link webpack
npm i -D webpack-cli
npm install --save react-star-ratings
npm install react-google-picker
npm audit fix


npm install --save react-chartjs-2 (maybe)
```